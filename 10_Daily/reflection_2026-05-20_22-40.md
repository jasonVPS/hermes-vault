# Background Cognition — 2026-05-20 22:40

- **Stale cron error flags:** Three script-only jobs (`Vault Graph`, `Self-Introspect`, `Metrics Report`) show `last_status: error` in `jobs.json`, yet all run successfully when invoked manually. Registry state only updates on scheduled ticks, not manual runs. Next scheduled runs (04:00, 05:00, 06:00) should clear the flags.
- **Log check clean:** No FATAL or ERROR entries. Gateway logs contain only routine auxiliary-provider payment/credit warnings (openrouter, nous) with automatic health-cycling — expected behavior.
- **Vault healthy:** Git working tree clean, last sync commit at 22:35. Disk usage stable at 15%.

**Action:** Monitor the three evolution scripts after their next scheduled runs to verify `last_status` flips to `ok`. No intervention needed.

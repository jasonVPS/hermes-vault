---
date: 2026-05-22
time: 06:20
tags: [background-cognition, cron, ssh, vault]
---

- **`hermes-vault-sync` fails every minute** with SSH `Permission denied (publickey)`. The `Vault Auto-Sync` (`vault-sync.py`) job is healthy (`last_status: ok`), making the failing `vault-sync.sh` redundant and noisy.
- **`Daily Health Check`** (2026-05-21) intentionally exited 1 with "CRITICAL issues found." Today's run is due at 06:30.
- **`Vault Graph`**, **`Self-Introspect`**, and **`Metrics`** are healthy anomaly reports (broken links: 239, config diff: 1, log errors: 8) — not crashes.

**Action:** Either fix `vault-sync.sh` to use `GIT_SSH_COMMAND="ssh -i /opt/data/home/.ssh/id_ed25519_vault"` in the minimal cron env, or remove the redundant `hermes-vault-sync` job to stop the per-minute error spam.

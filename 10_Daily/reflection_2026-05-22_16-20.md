# Background Cognition Reflection

**Observations**

- Three jobs currently show `last_status: error` — **Daily Health Check**, **Vault Graph**, and **Self-Introspect** — but all `last_error` fields contain structured anomaly reports (`CRITICAL issues found`, `Broken: 239, Orphans: 1`, `Diffs: 1`) rather than tracebacks. These are healthy intentional `exit(1)` signals, not crashes.
- Gateway Watchdog and both vault-sync jobs are running cleanly every 2 min / 1 min respectively. Vault git tree is fully committed and pushed to origin/main.
- No user-initiated session in the last 10 min; the most recent session is this Background Cognition run itself.

**Action Item**

- [ ] Evaluate whether `vault-graph.py` and `self-introspect.py` should exit 0 with report-on-stdout only, reserving exit 1 for actual failures, to prevent `last_status: error` churn in downstream health checks.

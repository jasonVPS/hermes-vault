## Observations

- **New `BTC Trading Agent` job (created 09:53) is running in LLM mode every 1 minute** (`no_agent: false`, script `run_trading.sh`). The task is deterministic (execute a shell script that runs a Python trading bot) — it does not need reasoning or summarization. At ~1,440 LLM invocations/day this will exhaust provider quota and likely trigger HTTP 429/503 rate limits on the gateway.
- **`Vault Graph`, `Self-Introspect`, and `Metrics Report` show `last_status: error`** — but their `last_error` strings contain intentional anomaly-report markers (`Broken:`, `Diffs:`, `Errors:`). These are healthy scripts exiting code 1 by design to surface issues via cron delivery; no manual intervention needed.
- **Vault git is clean**: no uncommitted files and no unpushed commits.

## Action Item

- [ ] **Convert `BTC Trading Agent` to `--no-agent` mode.** It is a script-only task; update `jobs.json` (`"no_agent": true`) or recreate the job with the `--no-agent` flag to stop unnecessary LLM burn.

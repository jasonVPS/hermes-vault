# Reflection — 2026-05-23 10:00

## Observations

- **New BTC Trading Agent** (id `59b70a6e3260`, created 09:53 today) runs **every 1 minute** with `no_agent: false`, spawning a full LLM subagent each tick solely to execute a shell script. This matches the "Accidental LLM Mode on Frequent Jobs" anti-pattern from the `hermes-evolution` skill — at ~60 runs/hour it risks provider rate limits (429/503) and burns API quota unnecessarily.
- All other jobs are healthy. The three `last_status: error` entries (Vault Graph, Self-Introspect, Metrics Report) are intentional `exit(1)` anomaly reports (markers present: `Graph JSON:`, `Report:`, `Broken:`, `Diffs:`, `Errors:`), not crashes.
- Vault git is clean: no uncommitted changes, no unpushed commits. Gateway logs show only benign WARNING patterns (tool-loop throttle from user sessions), no system anomalies.

## Action Item

- [ ] Convert `BTC Trading Agent` to `--no-agent` mode. Its prompt is pure script execution (`run_trading.sh` → `python3 trading_agent_v2.py`) with no reasoning, summarization, or decision-making required.

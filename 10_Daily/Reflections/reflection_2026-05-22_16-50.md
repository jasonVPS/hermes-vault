---
tags: [tech]
agent: Background Cognition
pillar: Metric-Driven / Multi-Agent Pool
---

## Observations
- `skill_manage` tool failure loop persists across sessions — gateway.log shows repeated `repeated_exact_failure_warning` (count ≥4) and `same_tool_failure_warning` (count ≥3). Root cause appears to be malformed autonomous skill-write attempts omitting required fields (`file_content`, frontmatter `name`).
- Four cron jobs report `last_status: error`, but all are intentional anomaly reports via `exit(1)` (markers: `Graph JSON:`, `Broken:`, `Orphans:`, `Metrics:`, `Diffs:`). No crashes.
- Vault git is clean: no uncommitted changes, no unpushed commits.

## Action Item
- [ ] Investigate which cron session or autonomous agent is triggering the malformed `skill_manage` calls and block or fix the pattern to prevent API quota waste and log noise.

## Siehe auch
- [[_meta/index/MOC|Master of Ceremonies]]

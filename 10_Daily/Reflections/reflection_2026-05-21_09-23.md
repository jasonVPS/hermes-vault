---
title: "Background Reflection 2026-05-21 09:23"
date: "2026-05-21"
tags: [reflection, system, cron, productivity, tech]
---

- `Daily Health Check` is falsely flagging `Vault Graph` and `Metrics Report` as CRITICAL because they intentionally exit 1 to report anomalies (233 broken links, 7 error statuses). This matches the documented "crying wolf" cascade pitfall.
- Manual runs of both scripts confirmed they are healthy; their `last_status: error` entries in `jobs.json` are stale and will clear at their next scheduled ticks.
- Gateway logs show only yesterday's stale tool warnings; vault git is clean, synced, and disk is at 15%.

**Action:** Patch `/opt/data/scripts/daily-health-check.py` to inspect `last_error` content and ignore anomaly-reporting exit-1 jobs.

## Siehe auch
- `Productivity Index`

---
title: "Background Cognition — 2026-05-21 14:40"
date: "2026-05-21"
tags: [reflection, cron, background-cognition, productivity, tech]
---

- **Daily Health Check (06:30) exited 1**, but its own report shows only healthy anomaly-reporting jobs (Vault Graph, Metrics Report doing intentional `exit(1)` for broken links / collected stats) plus benign gateway aux-client rate-limit warnings (`payment / credit error` from openrouter/nous). The health-check logic is conflating intentional anomaly reports with real crashes.
- **Previous Background Cognition runs had tool errors** in the last few hours: `execute_code` NameError (`error_jobs` undefined), `read_file` on non-existing reflection note, `terminal` called with `None` command. These caused silent run failures in past ticks without alerting.
- **Vault is clean**: git up to date, last sync 14:34; no uncommitted changes.

**Action item:** Tune `daily-health-check.py` to skip anomaly markers (`Graph JSON:`, `Report:`, `Broken:`, `Orphans:`, `Metrics:`, `Drift detected`) and benign log patterns (`marking .* unhealthy for .* payment / credit error`) so it only exits 1 on actual crashes or resource failures (gateway down, disk full, vault-sync failure).
## Siehe auch
- `Productivity Index`

---
title: "Background Cognition — 2026-05-21 11:10"
date: "2026-05-21 11:10"
tags: [reflection, system, cron, productivity, tech]
---

- **3 jobs show `last_status: error`, but all are intentional anomaly reports** — Vault Graph (233 broken links), Metrics Report (7 errors), and Daily Health Check (flagging the first two as "critical"). No actual crashes.
- **Health Check creates a false-positive cascade** — it treats `exit(1)` from Vault Graph and Metrics as failures, then itself exits 1, compounding noise. Per skill Pitfall #10, it should inspect `last_error` for markers like "Broken:" / "Report:" before declaring critical.
- **Broken links trending up** — 219 → 233 in the vault graph. Most are reflection notes linking to nonexistent `[[wiki-links]]` / `[[internal_links]]` targets. Orphans are at 0, so the graph structure is otherwise solid.

**Action:** Patch `daily-health-check.py` to detect anomaly-report marker strings in `last_error` and categorize them separately, avoiding the cascading critical alert.

## Siehe auch
- [[40_Areas/productivity-index.md|Productivity Index]]

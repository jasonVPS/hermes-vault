---
title: "Background Cognition Reflection 2026-05-21 07:22"
date: "2026-05-21"
tags: [reflection, system, cron, productivity, tech]
---

# Background Cognition — 2026-05-21 07:22

- **Setup Watchdog** (job `4c873f013b1d`) is in `error` due to `cp: Text file busy` reinstalling `blogwatcher-cli` at 07:00. Root cause: `command -v blogwatcher-cli` likely fails in the minimal cron PATH, triggering unnecessary reinstall every 30 min. When the binary is open by a concurrent process, the copy fails.
- **Daily Health Check** also flagged `error`, but that is a cascading false positive: it exits 1 because it sees Vault Graph and Metrics Report as `error`, yet both are intentionally designed to exit 1 on anomalies (broken links, error counts). This creates a "crying wolf" loop.
- **Auxiliary provider issues** (nous/openrouter payment errors) in gateway.log stopped after 23:43 yesterday — current primary provider (ollama-cloud) is healthy.

**Action item:** Fix `setup-check.sh` to use the absolute path `/opt/data/home/.local/bin/blogwatcher-cli` instead of `command -v`, so it stops false-reinstalling every 30 minutes.

## Siehe auch
- `Productivity Index`

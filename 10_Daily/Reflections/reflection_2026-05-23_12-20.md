---
tags: [daily, meta, reflection, system, tech]
---

## Observations

- Metrics Report (06:00) flagged "4 failed cronjobs", yet 3 of them — Vault Graph, Self-Introspect, and Metrics itself — intentionally exit 1 to surface anomaly reports (broken links, drift, error counts). Their `last_error` fields contain healthy markers, not tracebacks. This inflates the failure count and obscures real crashes.
- Vault Graph detected 439 broken wiki-links and 4 orphan notes. These numbers are stable day-to-day, suggesting systemic link rot rather than new damage.
- Gateway and setup logs show only benign throttle warnings from this very session (repeated `skill_manage` / `terminal` failures). No persistent crashes.

## Action Item

- [ ] Update `metrics.py` or `daily-health-check.py` to parse `last_error` for anomaly-report markers (`Broken:`, `Drift detected`, `Report:`) and exclude intentional `exit(1)` scripts from the "failed cronjobs" tally so only genuine crashes trigger alerts.

## Siehe auch

- [[10_Daily/2026/05/2026-05-23]]

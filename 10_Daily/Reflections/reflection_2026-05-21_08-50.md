---
title: "Reflection 2026-05-21 08:50"
date: "2026-05-21 08:50"
tags: [reflection, system, background-cognition, productivity, tech]
---

# 🧠 Background Cognition — 2026-05-21 08:50

## Observations
- **Cascading false positives:** `daily-health-check.py` exited `1` because it found Vault Graph and Metrics Report in `last_status: error`. Those two scripts intentionally exit `1` to report anomalies (broken links / error counts). Result: health check itself becomes a false alarm.
- **API rate limits:** `gateway.log` shows repeated `HTTP 429` (too many concurrent requests) from ollama-cloud. This is transient but increasing — could be cron density or auxiliary client retries.
- **Vault state:** Clean git status, no uncommitted changes. Disk at 15%.

## Action Item
> Tune `daily-health-check.py` to parse `last_error` content and skip jobs whose error message matches the structured-report pattern (e.g. "Broken: N, Orphans: N" or "Notes: N, Jobs: N, Errors: N"). Only treat actual tracebacks or repeated failures as critical.

## Siehe auch
- `Productivity Index`

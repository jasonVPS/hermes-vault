---
tags: [tech]
---

# Reflection 2026-05-22 08:10

- **jobs.json shows 4 errors**, but all are intentional `exit(1)` from `vault-graph`, `self-introspect`, and `metrics` (anomaly markers present). The *Daily Health Check* itself then exits 1 because it treats every `last_status: error` as a crash, creating a self-referential false-positive loop.
- **gateway.log** has `same_tool_failure_warning; count=4` for `skill_manage` in the 07:16 Telegram session — a benign tool-loop warning, not a background anomaly.
- **Vault git is clean**, committed at 08:05. No uncommitted or unpushed changes.

**Action item:** Patch `self-introspect.py` and/or `health-check.py` to skip anomaly-report markers (`Graph JSON:`, `Report:`, `Broken:`, etc.) so intentional script reports don't cascade into health-check alerts.

## Siehe auch
- [[_meta/index/MOC|Master of Ceremonies]]

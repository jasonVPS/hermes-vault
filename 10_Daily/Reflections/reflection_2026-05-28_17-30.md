---
title: "Background Reflection 2026-05-28 17-30"
date: "2026-05-28"
tags: [system, background-cognition, reflection]
---

# Background Reflection -- 2026-05-28 17-30

## Observations

- `vault-sync.py` points to `/opt/data/obsidian-vault` (non-existent), so it never syncs; the actual vault lives at `/opt/data/home/hermes-vault`. This script is still registered as a cronjob (`Vault Auto-Sync`) and appears healthy because it exits 0 on "Keine Änderungen" for the empty repo, masking that it does nothing.
- `vault-auto-commit.sh` has the correct path but only commits changed tracked files; its `git add -A` is reached only if there are already tracked modifications. Untracked RSS files (today: 7 notes) accumulate silently because the `git diff --cached --quiet` short-circuit exits before adding them.
- `vault-sync.sh` (correct path) ran at 17:02 and saw no tracked changes, so it also skipped the untracked files. Same for `hermes-vault-sync` (same script) at 17:30.

## Action Item

- [ ] Fix `vault-sync.py` path to `/opt/data/home/hermes-vault` or remove the job if `vault-sync.sh` is the canonical sync.
- [ ] Fix `vault-auto-commit.sh` so untracked files are always added: move `git add -A` before the diff check.

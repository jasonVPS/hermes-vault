---
tags: [tech, daily, reflection, system, meta]
date: 2026-05-20T13:10Z
source: background-cognition
type: reflection
---

- No cronjob errors, no uncommitted vault changes, disk at 15%.
- gateway.log shows 2 recurring script warnings: `AttributeError: 'str' object has no attribute 'get'` when parsing jobs.json (iterates dict keys instead of `.jobs` list), and a bash syntax error in a frontmatter parser. Also `jq` missing in one script path (`/opt/data/jobs.json` vs `/opt/data/cron/jobs.json`).
- Transient 503 overloads from ollama-cloud on `nitter-to-vault` job — provider-side, self-recovered.

**Action:** Patch the jobs.json parser and frontmatter script to eliminate warning noise. Non-urgent.

## Siehe auch

- [[_meta/index/MOC|Master of Ceremonies]]
---
title: "Background Cognition Reflection 2026-05-23 10:10"
date: "2026-05-23"
tags: [system, reflection, meta, tech]
---

# Background Cognition — 2026-05-23 10:10

## Observations
- New cronjob **"BTC Trading Agent"** (created ~09:53) runs every 1 minute in LLM mode (`no_agent: false`) only to execute `run_trading.sh`. This violates the anti-pattern rule: deterministic shell scripts should use `--no-agent` to avoid rate limits.
- Vault Graph, Self-Introspect, and Metrics Report show `last_status: error` but all are **intentional exit(1) anomaly reports** (Broken links, drift detected, errors count). No actual crashes.
- Gateway logs show only benign user-session tool failures (sudo denied, missing module in sandbox, skill_manage identical strings). No system-level anomalies.

## Action Item
- [ ] Recommend converting **BTC Trading Agent** to `--no-agent` mode since it only runs a shell script (`run_trading.sh`) and does not require LLM reasoning.

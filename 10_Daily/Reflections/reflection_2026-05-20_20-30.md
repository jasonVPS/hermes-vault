---
tags: [tech, daily, reflection, system, meta]
---
- Three cron jobs (`Vault Graph`, `Self-Introspect`, `Metrics Report`) show `last_status: error` in `jobs.json`, but manual runs confirm they are functional. `vault-graph.py` and `metrics.py` intentionally exit 1 when anomalies are detected (broken links / log errors), which leaves stale error flags until the next scheduled tick passes.
- Vault has 8 uncommitted RSS notes from today — normal feed ingestion; no sync failure.
- Gateway log shows routine security-scan warnings (denied `patch` tool, terminal exit -1), not crashes.

**Action item:** Review whether `vault-graph.py` and `metrics.py` should switch from `sys.exit(1)` to `sys.exit(0)` with a stdout summary, so cron registry doesn't flag them as broken on every anomaly cycle. Reserve exit 1 for actual script crashes.

## Siehe auch

- [[_meta/index/MOC|Master of Ceremonies]]

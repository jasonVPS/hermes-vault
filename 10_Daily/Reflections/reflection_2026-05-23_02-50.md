---
tags: [tech]
---

# Background Cognition — 2026-05-23 02:50

## Observations
- **Stale error flags mask self-healing.** Daily Health Check (job `bc91e861`) still shows `last_status: error` from yesterday 06:30 because it intentionally exits 1 when issues are found. The vault-sync SSH `Permission denied` it flagged has already resolved (current status: `ok`, last run 02:50). This means Background Cognition must always cross-check `last_error` against a live manual run before treating any script as broken.
- **Provider overload is transient noise.** Gateway log shows clustered HTTP 503 errors (`kimi-k2.6 temporarily overloaded`) and timeouts overnight. These are provider-side transients; no system-level action required. The gateway retry logic handles them automatically.
- **System state is otherwise healthy.** All 14 jobs are `scheduled` or `ok`, vault git is clean, disk is at 16%, and no uncommitted or unpushed changes.

## Action Item
- [ ] Consider adding a "staleness timestamp" or `last_resolved_at` field to cron jobs so that health checks and background agents can distinguish between active failures and already-healed issues without manual cross-checks.

## Siehe auch
- [[_meta/index/MOC|Master of Ceremonies]]

---
tags: [productivity, tech]
---

- **Anomaly reports, not crashes**: Daily Health Check and Vault Graph both show `last_status: error`, but their `last_error` fields contain healthy structured reports (`CRITICAL issues found` and `Broken: 219, Orphans: 2`). These are intentional `exit(1)` signals, not failures.
- **Vault sync backlog**: One uncommitted file in vault: `20_Notes/RSS/2026-05-21/Pinned_codex_app_...`. Next vault-sync tick (every 10 min) should pick it up.
- **Gateway transient**: `gateway.log` shows auxiliary-client rate-limit warning (`marking nous unhealthy for 60s`). Known benign pattern; no action needed.

## Siehe auch
- `Productivity Index`

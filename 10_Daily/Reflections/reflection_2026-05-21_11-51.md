---
date: "2026-05-21 11:51"
tags: [bg-cognition, reflection, system, productivity, tech]
---

# Background Reflection — 2026-05-21 11:51

- **Daily Health Check is crying wolf:** It correctly identifies Vault Graph and Metrics as *intentional* exit-1 anomaly reporters (2 jobs), yet still exits 1 itself. This creates a cascading stale-error in `jobs.json`. The health-check note at 09:24 even labels them "anomaly-reporting" — the script should exit 0 when only anomaly reporters are found.
- **219 broken vault links persist:** The vault-graph daily report flags this consistently. Two orphan notes also exist. These are structural debt, not crashes.
- **Gateway payment warnings:** `openrouter` and `nous` marked unhealthy due to payment/credit errors. These are provider-side, temporary, and already handled by automatic 60s backoff.

**Action:** Fix `self-introspect.py` or `health-check.py` logic to distinguish intentional anomaly reports from genuine crashes and exit 0 when no true failure exists.

## Siehe auch
- `Productivity Index`

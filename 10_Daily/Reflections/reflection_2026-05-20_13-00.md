---
tags: [tech, daily, reflection, system, meta]
---
# Background Cognition — 2026-05-20 13:00

- **Path drift in nitter-to-vault**: gateway.log shows `python3: can’t open file '/opt/data/nitter-to-vault.py'` during the 12:31 run. The file actually lives at `/opt/data/scripts/nitter-to-vault.py`. The cron job still reports `last_status: ok`, so the failure may be silent or triggered by a secondary subprocess. The job script path should be absolute (`/opt/data/scripts/nitter-to-vault.py`) or the cron resolver needs inspection.
- **Auxiliary provider payment warnings**: `openrouter` and `nous` were marked unhealthy for 60 s due to a payment/credit error. This is the second time in the last ~6 hours. If this persists, provider keys or credit balances should be verified.
- **Overall health**: All 10 cron jobs are green (`last_status: ok`), vault git is clean ahead of origin, disk at 15 %. Nothing critical.

**Action:** Update the nitter-to-vault cron script field to an absolute path (`/opt/data/scripts/nitter-to-vault.py`) to eliminate the file-not-found error on the next run.

## Siehe auch

- [[_meta/index/MOC|Master of Ceremonies]]

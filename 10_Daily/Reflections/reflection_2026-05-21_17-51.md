---
tags: [productivity, tech]
---

# Background Cognition Reflection

- **Health Check loop detected:** `Daily Health Check` exited 1 because it flags intentional anomaly reports (`vault-graph.py`, `metrics.py`) as failures. Those scripts legitimately exit 1 to signal data (broken links, error counts), not crashes.
- **Stale error state:** `jobs.json` retains `last_status: error` until the next scheduled tick passes, causing each health check run to re-escalate the same flagged jobs and re-exit 1.
- **Log noise benign:** Single provider timeout in `gateway.log`; no active system failures.

**Action item:** Patch `self-introspect.py` / health-check script to inspect `last_error` for known anomaly markers (`Broken:`, `Orphans:`, `Metrics:`, `CRITICAL issues found`) and skip them before declaring a critical failure. See `vps-cron-maintenance` skill section 12 for exact marker strings and `is_anomaly_report()` logic.

## Siehe auch
- [[40_Areas/productivity-index.md|Productivity Index]]

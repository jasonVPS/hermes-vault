# Background Cognition Reflection — 2026-05-20 20:41

- **Registry drift detected**: 3 cronjobs (`Vault Graph`, `Self-Introspect`, `Metrics Report`) show `last_status: error` with `last_run: null`. Manual runs confirm all scripts execute correctly — `vault-graph.py` and `metrics.py` intentionally exit 1 to signal anomalies (broken links / log errors), while `self-introspect.py` exits 0. The `last_run: null` indicates they have not yet executed on their first scheduled tick; registry state will self-correct at next run.
- **Log warnings are self-generated**: `errors.log` and `agent.log` anomalies stem from the Background Cognition agent's own security-scan blocks (`patch` and `terminal` denied by background review). These are expected constraints, not failures.
- **Action item**: No action required. Avoid premature job recreation when `last_run` is `null` — verify with `python3 /opt/data/scripts/<name>.py` first.

---
tags: [background-cognition, cron, anomaly]
date: 2026-05-20
---

# Background Cognition Reflection — 13:30

- **nitter-to-vault cron job path mismatch**: job looks for `/opt/data/nitter-to-vault.py`, but the actual script lives at `/opt/data/scripts/nitter-to-vault.py`. Every run fails with `No such file`.
- **Command `python` vs `python3`**: another recent error shows `/usr/bin/bash: line 3: python: command not found`, suggesting a hardcoded `python` call where `python3` is required.
- **Disk healthy**: 15% used on /opt/data, all vault-sync and gateway jobs report `last_status: ok`, vault is clean (no uncommitted/unpushed changes).

## Action
Fix `script` field in `jobs.json` for nitter-to-vault from `nitter-to-vault.py` to `scripts/nitter-to-vault.py` and ensure interpreter is `python3` not `python`.

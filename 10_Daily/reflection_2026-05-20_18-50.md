- **Memory store at 89% capacity** (1,232/1,375 chars). Memory writes will start failing soon. Need to review and prune or replace entries.
- **3 evolution scripts flagged `last_status: error`** (Vault Graph, Self-Introspect, Metrics). All intentionally exit(1) on anomaly detection — working as designed, but registry state will persist until next scheduled run.
- **Vault git is clean** — no uncommitted or unpushed changes.

**Action:** Review `memory` entries and remove stale items to free space.
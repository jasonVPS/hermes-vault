---
title: "Background Cognition Reflection"
date: "2026-05-23 12:50"
tags: [system, reflection, meta]
---

# Reflection 2026-05-23 12:50

## Observations
- 3 cronjobs show `last_status: error`: Vault Graph, Self-Introspect, Metrics Report. All are **intentional anomaly reports** (broken links, config drift, error count), not crashes — healthy behavior.
- Vault git is clean: zero uncommitted or unpushed files. Live state is healthy.
- `gateway.log` and `setup-check.log` show only benign WARNING patterns (auxiliary client rate-limit, tool loop from user session). No system crashes.
- New `BTC Trading Agent` cronjob (created ~09:53 today) is running **without `--no_agent`**, yet its entire prompt is "run this shell script." It fires every minute and has already completed 151 LLM-driven runs. This wastes API quota and invites rate-limiting.

## Action Item
- [ ] Switch `BTC Trading Agent` to `--no-agent` mode: `hermes cron remove 59b70a6e3260` then recreate with `--no-agent --script run_trading.sh --deliver origin`. The script itself handles all trading logic; no reasoning or tool use is needed in the cron wrapper.

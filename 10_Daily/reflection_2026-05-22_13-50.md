---
tags: [tech]
---

# Background Cognition — 2026-05-22 13:50

- Four maintenance jobs (Daily Health Check, Vault Graph, Self-Introspect, Metrics Report) show `last_status: error`, but `last_error` contains structured anomaly reports (intentional `exit(1)`), not crashes. Stale status will clear at next scheduled tick. No action needed.
- Gateway log shows a tool-loop warning: `skill_manage` failed 3–4 consecutive times around 13:20 today. Not yet recurring; monitor upcoming sessions.
- Vault git is clean. Provider rate-limit warnings in `agent.log`/`errors.log` are transient and benign.

**Action item:** Watch for repeated `skill_manage` tool-loop warnings in next few sessions. If it recurs, inspect the specific cron session logs.

## Siehe auch
- [[_meta/index/MOC|Master of Ceremonies]]

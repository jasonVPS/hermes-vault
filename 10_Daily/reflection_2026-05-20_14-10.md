---
date: 2026-05-20T14:10:00+00:00
type: reflection
source: background-cognition
---

# Hintergrund-Reflexion 14:10 UTC

- **nitter-to-vault** erzeugt wiederholte Gateway-Warnings: Agent versucht `/opt/data/nitter-to-vault.py` statt `/opt/data/scripts/nitter-to-vault.py`. Script existiert korrekt, Job-Status bleibt `ok` — reines Log-Noise.
- Alle anderen Cron-Jobs sauber (`last_status: ok`). Vault ist clean (keine uncommitted/unpushed Changes). Disk 15 % belegt.
- Letzte aktive Session war dieser Background-Cognition-Lauf selbst — keine Nutzer-Interaktion in den letzten 10 Min.

**Action:** `nitter-to-vault` auf `no_agent: true` umstellen (Script läuft standalone) oder Prompt um expliziten Pfad ergänzen, um Warnungen zu eliminieren.

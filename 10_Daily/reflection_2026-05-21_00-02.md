---
tags: [tech, daily, reflection, system, meta]
---
# Background Cognition Reflection — 2026-05-21 00:02

- **Stale error flags:** `vault-graph`, `self-introspect`, and `metrics` all show `last_status: error` in `jobs.json`, yet manual runs succeed with exit 0. These are first-run artifacts from yesterday; they will auto-clear at their next scheduled ticks (04:00, 05:00, 06:00).
- **Auxiliary provider warnings:** `gateway.log` has repeated payment/credit warnings for `nous` and `openrouter` (non-fatal, 60s cooldowns). No fatal or crash-level anomalies.
- **Vault sync gap:** Last vault-sync commit was ~25 min ago (`ba57e47`), 5 new untracked files (daily reports + RSS notes) waiting. Within normal range for 10-min sync interval.

**Action:** None — monitor whether the three error flags clear after their scheduled runs. If they persist post-06:00, investigate the script exit-code logic.

## Siehe auch

- [[_meta/index/MOC|Master of Ceremonies]]

---
title: "Background Cognition Reflection"
date: "2026-05-21 07:40"
tags: [cognition, cron, health-check]
---

- **Cascading false positives:** `Daily Health Check` flagged `Vault Graph` and `Metrics Report` as crashed because both exit with code 1 when anomalies are found. Their `last_error` fields contain structured reports (`Broken: 219`, `Errors: 7`), not tracebacks.
- **Gateway log noise:** Three tool failures in the last few hours (patch denied by security scan, execute_code syntax error from escaped backslashes in reflection script, terminal pipe-to-interpreter blocked). These are sandbox/security guardrails firing, not system failures.
- **Vault sync is clean:** No uncommitted or unpushed changes.

**Action item:** Fix `daily-health-check.py` to parse `last_error` content and distinguish between structured anomaly reports (healthy) vs. actual tracebacks/crashes (unhealthy) before declaring CRITICAL issues.
# Background Cognition Reflection
*2026-05-21 00:40 UTC*

- **Stale error flags in 3 script jobs:** `vault-graph`, `self-introspect`, and `metrics` all show `last_status: error` in `jobs.json`, but manual runs succeed. Their exit code 1 is intentional (broken links / config drift / error counts detected). These are healthy anomaly signals, not crashes. Flags will clear at next scheduled tick.
- **Auxiliary provider credit warnings in gateway.log:** `openrouter` and `nous` marked unhealthy briefly due to payment/credit errors. This is a warning, not fatal — gateway continues operating on primary provider.
- **Vault git clean:** No uncommitted or unpushed changes.

**Action item:** Add a pre-flight check in Background Cognition to distinguish intentional `exit(1)` anomaly reports from actual crashes by reading `last_error` content before triggering manual verification.

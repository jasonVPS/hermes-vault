# Background Cognition — 2026-05-21 13:20

- **Stale error flag**: `Daily Health Check` shows `last_status: error` from 06:30, but a manual run just now returned “All clear.” The flag will clear at the next scheduled tick (06:30 tomorrow).
- **Vault Graph anomaly report**: `vault-graph.py` exited 1 at 04:00 with 219 broken links and 2 orphans. This is by-design behavior (intentional exit 1 to surface link rot), not a crash.
- **Auxiliary provider warnings**: `gateway.log` shows transient payment/credit errors for `nous` and `openrouter`. These are auxiliary-model fallbacks; the primary provider (`ollama-cloud`) is unaffected and the gateway is running normally.

**Action item:** Review the 219 broken vault links when convenient — likely a batch of renamed or moved notes.

- **Stale error flags:** `Daily Health Check` and `Vault Graph` both show `last_status: error`, but both are yesterday's runs. Health Check found "CRITICAL issues" (likely intentional exit-1); Vault Graph reported `Broken: 219, Orphans: 2` (healthy anomaly report). Next runs today at 04:00 and 06:30 will clear the flags if they succeed.
- **Gateway log:** One genuine `execute_code` error with a Python traceback (`json.loads` failure in a script). This is a real tool execution crash, not a benign rate-limit warning. The other log lines are the usual transient `marking ... unhealthy for 60s (payment / credit error)`.
- **Vault git:** Clean. No uncommitted changes.

**Action item:** Investigate the `execute_code` JSON parse traceback in `gateway.log` — identify which agent/script triggered it and whether it's recurring.

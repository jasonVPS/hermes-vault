---
tags: [daily, meta, reflection, system, tech]
---

# Background Cognition – 2026-05-23 12:40

## Observations

- **BTC Trading Agent runs in LLM mode every minute.** Job `59b70a6e3260` (`BTC Trading Agent`) has `no_agent: false` and executes a deterministic bash script (`run_trading.sh`) on a 1-minute interval. This burns API tokens on every tick and risks provider rate-limiting (429/503). Previous cognition ticks already noted this, but the job was never converted to `--no-agent`.
- **Three maintenance jobs show intentional `exit(1)` reports, not crashes.** Vault Graph (`Broken: 439, Orphans: 4`), Self-Introspect (`Diffs: 1`), and Metrics Report (`Errors: 8`) all use healthy anomaly-reporting patterns. Live checks confirm vault git is clean and sync is current.
- **Gateway logs are clean of real failures.** Recent `grep -i error` hits are benign transients: `APITimeoutError`, tool-loop warnings for `skill_manage` and `terminal`. Tail of the log shows normal gateway operation.\n## Action Item

- [ ] **Convert BTC Trading Agent to `--no-agent`.** It does not need reasoning, web search, or tool selection — it only runs a shell script. Recreate the job with `hermes cron remove 59b70a6e3260` then `hermes cron create 'every 1m' 'BTC Trading Agent' --no-agent --script run_trading.sh --deliver origin`.

## Siehe auch

- [[10_Daily/2026/05/2026-05-23]]

---
title: "Background Reflection 2026-05-21 18:20"
date: 2026-05-21
tags: [reflection, system, auth, productivity, tech]
---

- `gateway.log` shows repeated `Permission denied` on `/opt/data/auth.json` (Errno 13). Corrupt file already preserved at `.corrupt`. This is a real issue, not transient.
- Rate limit hit on ollama-cloud (`HTTP 429: too many concurrent requests`) during title generation, causing auxiliary title gen to fail. Likely triggered by concurrent cronjobs + user chat.
- Three cronjobs show `last_status: error`, but all are intentional anomaly reports (vault-graph broken links, metrics errors count, health-check flagging those). Cascade false-positive pattern is active.

**Action item:** Fix `auth.json` permissions (`chmod 644 /opt/data/auth.json` or check ownership) so gateway stops failing auth reads.

## Siehe auch
- [[40_Areas/productivity-index.md|Productivity Index]]

---
date: "2026-05-22 14:20"
tags: [reflection, system, cron]
---

# Reflection — 2026-05-22 14:20

- `hermes-vault-sync` hit a `Permission denied (publickey)` git error this morning (06:30) but recovered by 14:21. Likely transient SSH/key resolution issue under minimal cron env.
- Three anomaly-reporting scripts (vault-graph, self-introspect, metrics) intentionally exit 1 every day — this is expected, but the `last_status: error` flags persist in `jobs.json` and can be misread by downstream checks.
- Vault graph shows 239 broken links and 1 orphan. Link rot is accumulating; worthwhile to schedule a focused cleanup pass.

**Action:** Review whether `hermes-vault-sync` needs the explicit `GIT_SSH_COMMAND` export used by `vault-sync.sh` to prevent future transient SSH failures.

# Background Cognition — 2026-05-20 18:10

- **Three daily system scripts exited code 1** on their first run today: Vault Graph (1,199 broken links), Self-Introspect (1 config diff detected), and Metrics Report (7 log errors). All use exit(1) intentionally to flag anomalies, but with `deliver: local` this only pollutes `jobs.json` status. Registry state will stay `error` until the next scheduled success.
- **Memory store is at 89% capacity** (1,232/1,375 chars). The memory tool rejected a new entry. If it hits the hard limit, future corrections and environment facts will be silently dropped.
- **Gateway security scan flagged a HIGH severity pipe** (`cat | python3`) in a recent session. This is a pattern that can execute untrusted downloaded code.

**Action:** Audit memory entries — replace or remove stale ones to reclaim ~200 chars of headroom before the limit blocks new facts.

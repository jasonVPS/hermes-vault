- Stale cron error flags: Vault Graph and Self-Introspect both succeeded on manual run; `last_status: error` will clear at next scheduled tick.
- Uncommitted vault reports: `vault-graph_2026-05-20.md/json` and `self-introspect_2026-05-20.md` sit in working tree but are not committed.
- Action: run `git add -A && git commit -m "chore(system): daily graph + introspect reports"` in vault so sync can push.
---
title: "Reflection 2026-05-22 08:30"
date: "2026-05-22"
tags: [reflection, system]
---

# Reflection — 2026-05-22 08:30

- `hermes-vault-sync` hit a transient SSH failure at 06:30 (exit 128, `Permission denied (publickey)`). jobs.json now shows `ok`, so it recovered on the next tick, but this is the third occurrence this week.
- `vault-graph.py` reports 239 broken links and 1 orphan across 1165 notes — broken-link count is stable but high.
- `self-introspect.py` detected 1 config/skill drift diff; `metrics.py` tracked 8 gateway/cron errors in the last 24h.

**Action:** Review the `hermes-vault-sync` cron script for SSH key selection — likely `~/.ssh/config` `Host github.com` block is overriding the vault key in the minimal cron env (see VPS Cron Maintenance §2c). Override with `GIT_SSH_COMMAND` pointing explicitly to `id_ed25519_vault`.

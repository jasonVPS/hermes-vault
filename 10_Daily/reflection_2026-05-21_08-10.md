---
title: "Reflection 2026-05-21 08:10"
date: "2026-05-21"
tags: [reflection, system, meta]
---

- **Blocked tools in gateway logs** — Background agents attempted `patch` and `tail | python3`; both rejected by review rules (`patch` not whitelisted for background agents, pipe-to-interpreter flagged). These are not crashes, but they indicate agents are being prompted with modification patterns they can't execute.
- **`execute_code` backslash escape failures** — `datetime.strftime` strings with backslashes in f-strings are breaking inside the sandbox (slashes get double-escaped during shell transit). Use `write_file` or `browser_type`/`terminal` instead of heredoc-style `execute_code` for reflection filenames.
- **Vault Graph: 219 broken links, 2 orphans** — Intentional `exit(1)` by design, but link rot is accumulating steadily. Most broken targets are old reflection notes and RSS articles with stale `[[...]]` references.

**Action:** Review Background Cognition prompts to avoid tool calls that are known to be blocked (`patch`, piped heredocs); switch file-writing to `write_file` only.

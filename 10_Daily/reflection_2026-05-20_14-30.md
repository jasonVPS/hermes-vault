---
tags: [tech, daily, reflection, system, meta]
---
- **vault-graph.py is broken**: recurring `syntax error near unexpected token ')'` in gateway logs — triple-quoted f-string inside the script is corrupting bash `eval`. Also hit `AttributeError: 'str' object has no attribute 'get'`.
- **nitter-to-vault.py path drift**: cron or agents are referencing `/opt/data/nitter-to-vault.py` (missing) while the real file is at `/opt/data/scripts/nitter-to-vault.py`.
- **Vault has 2 uncommitted RSS notes** from today; no unpushed commits.

**Action:** Fix `vault-graph.py` by rewriting the problematic f-string block atomically and running `python3 -m py_compile` to verify.

## Siehe auch

- [[_meta/index/MOC|Master of Ceremonies]]

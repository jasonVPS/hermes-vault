---
title: "Background Cognition Reflection — 2026-05-22 23:40"
date: "2026-05-22"
tags: [reflection, background-cognition, system, tech]
---

## Observations

- `hermes-vault-sync` failed with `git@github.com: Permission denied (publickey)` at 06:30 (caught by Daily Health Check). `jobs.json` now shows `last_status: ok`, so the failure was transient, but it points to recurring SSH key resolution fragility in the minimal cron environment.
- `Vault Graph` (239 broken links, 1 orphan) and `Self-Introspect` (1 diff) are intentional anomaly reports (healthy exit-1 scripts), not crashes. The high broken-link count is persistent.
- `gateway.log` and `setup-check.log` show no active errors beyond benign `[Tool loop warning]` throttle noise.

## Action Item

- [ ] Harden `vault-sync.sh` with `export GIT_SSH_COMMAND="ssh -i /opt/data/home/.ssh/id_ed25519_vault -o StrictHostKeyChecking=no -F /dev/null"` at the top of the script to prevent future transient SSH auth failures in cron.

## Siehe auch
- [[_meta/index/MOC|Master of Ceremonies]]

- **Healthy anomaly exits**: `vault-graph`, `self-introspect`, `metrics` all show `last_status: error` but are intentionally exiting `1` to flag broken links, config drift, and 7 log errors. Reports generated normally. No action needed.
- **Real tool failure in gateway.log**: A prior session hit `skill_manage` error — `Could not find a match for old_string`. Patch target likely drifted after last edit. Not critical but leaves a skill in an unpatched state.
- **Vault clean**: No uncommitted or unpushed changes.

**Action:** Review the skill that failed `skill_manage` (check gateway log around timestamp) and verify whether the intended patch still needs applying.
